export interface StatsEarner {
  totalEarnings: number;
  earnerType: String;
  rating: number;
  vehicleType: String;
}

export interface StatsRider {
  totalSpent: number;
  totalTrips: number;
  rating: number;
}
